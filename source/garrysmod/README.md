# GMod specific files
The file names are exposed in filesystem_stdio.dylib :D<br>
So we keep their naming style<br>
We also include the interface classes here to avoid having to edit the sourcesdk-minimal so often<br>

All of these are internal so we got full freedom except the things in `public/`<br>
We **could** make it match GMod but we don't have to<br>